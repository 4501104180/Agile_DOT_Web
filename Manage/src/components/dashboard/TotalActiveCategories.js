import { Card, Typography } from '@mui/material';

const TotalActiveCategories = () => {
    return (
        <Card sx={{ p: 3 }}>
            <Typography variant='subtitle2'>Total Active Categories</Typography>
            <Typography variant='h4'>696</Typography>
        </Card>
    );
};

export default TotalActiveCategories;
